import scheduler as sc

if __name__ == '__main__':
    sched = sc.Scheduler()
    newline = sched.prompt()
    # while(1):
    #     newline = sched.prompt()
    #     print(newline)
    #     if newline == 'exit\n':
    #         break